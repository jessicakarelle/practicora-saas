import type { Metadata } from "next";
import { CheckCircle2 } from "lucide-react";
import { translate as t } from "@/i18n";
import { MarketingPageShell } from "@/components/marketing/page-shell";

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> { const {locale}=await params; return {title:t(locale,"marketing.changelog.metadata_title"),description:t(locale,"marketing.changelog.metadata_description")}; }
export default async function ChangelogPage({params}:{params:Promise<{locale:string}>}){const{locale}=await params;const versions=["74","73","72","71"];return <MarketingPageShell locale={locale} eyebrow={t(locale,"marketing.changelog.eyebrow")} title={t(locale,"marketing.changelog.title")} description={t(locale,"marketing.changelog.description")}><section className="mx-auto max-w-[900px] space-y-5 px-4 py-14 sm:px-6 lg:px-8">{versions.map(version=><article key={version} className="rounded-2xl border border-border bg-surface p-5 shadow-[var(--shadow-soft)] sm:p-6"><p className="text-xs font-bold uppercase tracking-[.12em] text-primary">{t(locale,`marketing.changelog.v${version}_date`)}</p><h2 className="mt-3 text-lg font-extrabold">{t(locale,`marketing.changelog.v${version}_summary`)}</h2><ul className="mt-5 grid gap-3 sm:grid-cols-2">{[1,2,3,4].map(item=><li key={item} className="flex gap-2.5 text-sm leading-6 text-muted-strong"><CheckCircle2 className="mt-1 size-4 shrink-0 text-success"/>{t(locale,`marketing.changelog.v${version}_item_${item}`)}</li>)}</ul></article>)}</section></MarketingPageShell>}
